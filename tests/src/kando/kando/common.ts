import type { CallableCell } from '@holochain-open-dev/tryorama';
import { Timestamp, ActionHash, Record, AppBundleSource, fakeActionHash, fakeAgentPubKey, fakeEntryHash, fakeDnaHash } from '@holochain/client';
